#include <iostream>

// 네임스페이스란 많은 라이브러리를 불러서 사용하다보면 변수명 중복이 발생할 수 있는데
// 이를 방지하기 위해서 변수명에 범위를 걸어놓은 것을 의미함
using namespace std;


int main() {
  string a;
  cin >> a;
  cout << a << endl;
  return 0;
}